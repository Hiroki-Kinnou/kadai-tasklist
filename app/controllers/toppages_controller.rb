class ToppagesController < ApplicationController
  def index
    #if logged_in?
      #@user = current_user
      #@task = current_user.tasks.build
      
      #@tasks = current_user.tasks.order('created_at DESC').page(params[:page]).per(10)
      
      #@task_edit = current_user.tasks.find_by(id: params[:id])
    #end
  end
end
